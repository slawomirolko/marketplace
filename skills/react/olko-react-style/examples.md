# Olko React Style Examples

Example report:

```text
React style violations:
  1. src/components/UserCard/UserCard.tsx:9 - useQuery called directly instead of a domain query hook (source: olko-react-style:React Query style)
```
