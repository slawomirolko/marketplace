# Olko React Testing Overview

Checks React test conventions plus project docs/adapters. Use for Vitest + Testing Library + MSW harness conventions, no-skip/no-silent-pass policy, network-boundary test doubles, parametrization, fixture reuse, React Query/React Hook Form testing conventions, manual e2e verification on the dev machine via the agent-browser (Vercel) tool, what-not-to-test (config contents, static structure), new-component test requirements, cross-boundary contract tests, and the five qualities of a good unit test.

Load `SKILL.md` for the canonical workflow and rule list.
