# Olko Commit Style Overview

Groups changed files by stack, runs documented style tools, and optionally delegates stack-specific style/architecture/test-convention checks through `uses`.

Load `SKILL.md` for the canonical workflow.
