# Olko Commit Style Edge Cases

- If a matching stack-specific skill is not declared in `uses`, run built-in document-based checks.
- Do not auto-load dependencies that are not declared in the project adapter.
