# Olko Docker Style Overview

Checks Dockerfile, `.dockerignore`, and Compose conventions plus project docs/adapters. Use for .NET multi-stage images, pinned base tags, cache-friendly restore layers, non-root runtime users, healthchecks, exec-form entrypoints, minimal runtime images, build-context hygiene, Compose secrets, service healthchecks, restart policy, volumes, and networking.

Load `SKILL.md` for the canonical workflow and rule list.
