# Olko Docker Style Examples

Example report:

```text
Docker convention violations:
  1. src/Api/Dockerfile:1 - base image uses latest tag (source: olko-docker-style:Base Image Tagging)
  2. compose.yaml:42 - service has hardcoded secret in environment block (source: olko-docker-style:Docker Compose conventions)
```
