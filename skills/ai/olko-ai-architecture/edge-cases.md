# Olko AI Architecture Edge Cases

- Do not create, move, or delete files unless the user or parent workflow asks for changes.
- Discover surface names by convention (`apps/*`, `services/*`, `platform/*`) or docs; never hardcode them.
- Project docs override marketplace defaults.
