# Olko AI Architecture Overview

Checks the `ai/` tree against marketplace defaults: `context/`+`skills/`+`prompts/`+`templates/` layout, surface-split context files, surface-split skill directories, global vs surface separation, no stale/orphaned artifacts, and sync with codebase surfaces. Use for plan review, onboarding a new surface, or standalone ai-tree validation.

Load `SKILL.md` for the canonical workflow and rule list.
