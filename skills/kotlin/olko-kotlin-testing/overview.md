# Olko Kotlin Testing Overview

Checks Kotlin/Android test conventions plus project docs/adapters. Use for instrumentation vs unit tier rules, Gradle harness conventions (`./gradlew test` / `connectedCheck`), no-skip/no-silent-pass policy, MockK/fake test-double boundaries, parametrization, fixture reuse, Compose UI testing conventions (waitForIdle, LazyColumn scroll, unmerged tree), Windows Gradle config-cache hang handling, what-not-to-test (config contents, static structure), new-component test requirements, cross-boundary contract tests, and the five qualities of a good unit test.

Load `SKILL.md` for the canonical workflow and rule list.
