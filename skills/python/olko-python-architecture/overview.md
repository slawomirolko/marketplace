# Olko Python Architecture Overview

Checks Python architecture for changed `.py` files with marketplace defaults: package boundaries, dependency direction, generated-code ownership, source layout, runtime/dependency management, observability (OpenTelemetry), and gRPC/external-service client configuration. Use before commit/test gates, during plan review, or when delegated by olko-commit-style/olko-test/olko-plan-editor.

Load `SKILL.md` for the canonical workflow and rule list.
