# Olko Python Testing Overview

Checks Python test conventions plus project docs/adapters. Use for integration vs unit tier rules, pytest harness and `_integration` suffix, no-skip/no-silent-pass policy, monkeypatch/unit-only doubles, parametrization, fixture reuse, integration boundary rules, what-not-to-test (config contents, static structure), new-component test requirements, contract tests, and fail-fast tests.

Load `SKILL.md` for the canonical workflow and rule list.
