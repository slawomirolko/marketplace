# Olko Python Style Edge Cases

- Skip generated `*_pb2*` files unless docs require checking them.
- Do not install tools or add config; infer narrow verify commands only for already-configured project-local tools.
- Project docs override marketplace defaults; never enforce undocumented personal Python opinions.
