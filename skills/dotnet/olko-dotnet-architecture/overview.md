# Olko Dotnet Architecture Overview

Checks .NET architecture defaults plus project docs/adapters. Use for modular monolith boundaries, layer/dependency direction, EF/API/messaging/telemetry/saga/config/secrets rules, and project-specific architecture overrides.

Load `SKILL.md` for the canonical workflow and rule list.
