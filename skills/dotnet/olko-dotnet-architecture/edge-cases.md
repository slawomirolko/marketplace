# Olko Dotnet Architecture Edge Cases

- If project docs conflict with marketplace defaults, project docs win.
- If a rule contains project-specific names, move it to the project adapter or `AGENTS.md`.
- If no architecture command/test exists, run document-based inspection only.
