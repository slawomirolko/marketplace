# Olko Dotnet Style Overview

Checks .NET/C# style defaults plus project docs/adapters. Use for XML docs, method shape, naming, modern C#, performance style, EF style, generated-file skips, and `dotnet format` defaults.

Load `SKILL.md` for the canonical workflow and rule list.
