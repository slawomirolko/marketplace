# Olko Dotnet Testing Examples

Example report:

```text
.NET test convention violations:
  1. src/App/App.csproj:12 - InternalsVisibleTo exposes internals to a non-test project (source: olko-dotnet-testing:Visibility and InternalsVisibleTo)
```
