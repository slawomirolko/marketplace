# Olko Dotnet Testing Overview

Checks .NET test conventions plus project docs/adapters. Use for `InternalsVisibleTo`, timeout/reflection/test-double rules, smart enum coverage, saga serialization assertions, and fail-fast tests.

Load `SKILL.md` for the canonical workflow and rule list.
