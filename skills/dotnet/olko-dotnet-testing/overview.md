# Olko Dotnet Testing Overview

Checks .NET test conventions plus project docs/adapters. Use for unit vs integration tier rules, `InternalsVisibleTo`, reflection bans, timeout policy, Shouldly assertions, NSubstitute/unit-only doubles, no-skip/no-silent-pass policy, Respawn integration database resets, smart enum coverage, saga serialization assertions, and fail-fast tests.

Load `SKILL.md` for the canonical workflow and rule list.
